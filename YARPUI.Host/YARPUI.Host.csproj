<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>YARPUI.Host</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Consume the published package; switch back to a ProjectReference
         (..\YARPUI\YARPASUI.csproj) to test local library changes. -->
    <PackageReference Include="YA-RP-UI" Version="0.1.2" />
  </ItemGroup>

</Project>
