<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>YARPUI.Host</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Consume the local library while developing; switch back to the published package
         (<PackageReference Include="YA-RP-UI" Version="0.2.0" />) for release builds. -->
    <ProjectReference Include="..\YARPUI\YARPASUI.csproj" />
  </ItemGroup>

</Project>
